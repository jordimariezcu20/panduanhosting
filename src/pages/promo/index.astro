---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const entries = (await getCollection('promo')).sort(
  (a, b) =>
    new Date(b.data.lastUpdated ?? b.data.publishDate ?? '').getTime() -
    new Date(a.data.lastUpdated ?? a.data.publishDate ?? '').getTime()
);
---
<BaseLayout
  title="Promo & Diskon Hosting | PanduanHosting.id"
  description="Kumpulan promo dan diskon hosting web terbaru di Indonesia. Hemat lebih banyak dengan kupon eksklusif."
  canonical="/promo"
>
  <main class="max-w-5xl mx-auto px-4 py-12">
    <h1 class="text-3xl font-bold text-gray-800 mb-2">Promo & Diskon Hosting</h1>
    <p class="text-gray-500 mb-8">Kupon dan penawaran eksklusif hosting web Indonesia yang sedang aktif.</p>

    {entries.length === 0 ? (
      <p class="text-gray-400 text-center py-16">Belum ada promo aktif. Segera hadir!</p>
    ) : (
      <div class="grid md:grid-cols-2 gap-6">
        {entries.map((entry) => (
          <a href={`/promo/${entry.slug}`} class="block border border-gray-200 rounded-xl p-6 hover:shadow-md transition-shadow">
            <span class="text-xs font-semibold text-[#1565C0] uppercase tracking-wide">{entry.data.category}</span>
            <h2 class="text-lg font-bold text-gray-800 mt-1 mb-2">{entry.data.title}</h2>
            <p class="text-gray-500 text-sm line-clamp-2">{entry.data.description}</p>
            {entry.data.publishDate && (
              <p class="text-xs text-gray-400 mt-3">{new Date(entry.data.publishDate).toLocaleDateString('id-ID', { year: 'numeric', month: 'long', day: 'numeric' })}</p>
            )}
          </a>
        ))}
      </div>
    )}
  </main>
</BaseLayout>
